show variables like "%secure%";
-- La variable "secure_file_priv" determina el lugar donde se almacenan las copias de seguridad del servidor
use Fila1;
create table Alumnos (
	id int auto_increment primary key,
	nombre varchar(45)
);