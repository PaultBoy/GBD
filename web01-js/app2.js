let nAlumno=0;
console.log(datos)
while (nAlumno < datos.length) {
    console.log("Páginas: "+datos[nAlumno].paginas);
    nAlumno++;
}

for (let numAlumno = 0; numAlumno < datos.length; numAlumno++) {
    console.log(datos[numAlumno].calificacion)
    
}